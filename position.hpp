#pragma once

#include "area_id.hpp"

class Position {
    public:
        Position(AreaID area, Coord x, Coord y, Coord z)

    private:
        AreaID area;
        Coord x;
        Coord y;
        Coord z;
}
